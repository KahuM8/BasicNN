 to run this code in terminal type:
 
 run sh file in terminal with
 
 bash run.sh
 
 or 
 
 
 javac a2Part1.java LabelEncoder.java NeuralNetwork.java Rescaler.java Util.java -d bin

cd bin

java a2Part1


How to read the data:

after each line of "--------------------------------------------------------------------------------------------" is the output of a whole set of runTest calls

The first set of tests is the first feed forward pass and its relevant data then its continuation with backpropagation and its relevant data

after it will do the 100 epochs and then the test training data


